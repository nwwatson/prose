<% content_for(:title, "New Category — #{site_name}") %>

<div class="space-y-6">
  <h1 class="text-2xl font-bold text-gray-900">New Category</h1>
  <%= render "form", category: @category %>
</div>
