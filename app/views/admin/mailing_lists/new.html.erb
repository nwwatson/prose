<% content_for(:title, "#{t('admin.mailing_lists.new.title')} — #{site_name}") %>

<div class="space-y-6">
  <h1 class="text-2xl font-bold text-gray-900"><%= t('admin.mailing_lists.new.title') %></h1>
  <%= render "form", mailing_list: @mailing_list %>
</div>
