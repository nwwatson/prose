<% content_for(:title, "#{t('admin.membership_tiers.new.title')} — #{site_name}") %>

<div class="max-w-4xl">
  <h1 class="text-2xl font-bold text-gray-900"><%= t('admin.membership_tiers.new.title') %></h1>
  <div class="mt-6">
    <%= render "form", tier: @tier %>
  </div>
</div>
