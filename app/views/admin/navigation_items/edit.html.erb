<% content_for(:title, "#{t('admin.navigation_items.edit.title')} — #{site_name}") %>

<div class="space-y-6">
  <%= render "admin/shared/page_header", title: t("admin.navigation_items.edit.title") %>
  <%= render "form", item: @item %>
</div>
