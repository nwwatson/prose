<%# locals: (title:, action: nil) %>
<div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
  <h1 class="text-2xl font-bold text-gray-900"><%= title %></h1>
  <%= action %>
</div>
