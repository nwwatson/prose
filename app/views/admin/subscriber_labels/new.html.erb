<% content_for(:title, "#{t('admin.subscriber_labels.new.title')} — #{site_name}") %>

<div class="space-y-6">
  <h1 class="text-2xl font-bold text-gray-900"><%= t('admin.subscriber_labels.new.title') %></h1>
  <%= render "form", label: @label %>
</div>
