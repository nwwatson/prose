<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || site_name %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="<%= site_name %>">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%= json_ld_tag({ "@context": "https://schema.org", "@type": "Organization", name: site_name, url: root_url }) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <link rel="alternate" type="application/rss+xml" title="<%= site_name %> RSS Feed" href="<%= feed_path(format: :xml) %>">

    <%= font_stylesheet_tags %>
    <%= typography_style_tag %>

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-cream text-charcoal font-serif min-h-screen flex flex-col">
    <%# Masthead %>
    <header class="border-b border-gray-300 py-6">
      <div class="mx-auto max-w-3xl px-4">
        <div class="text-center">
          <%= link_to site_name, root_path, class: "font-display text-4xl font-bold tracking-tight text-charcoal hover:text-ink-blue transition-colors no-underline" %>
        </div>
        <nav class="mt-3 flex justify-center space-x-6 text-sm">
          <%= link_to "Home", root_path, class: "text-gray-600 hover:text-charcoal" %>
          <%= link_to "About", about_path, class: "text-gray-600 hover:text-charcoal" %>
        </nav>
      </div>
    </header>

    <%# Flash messages %>
    <% if notice %>
      <div class="mx-auto max-w-3xl px-4 mt-4">
        <div class="rounded-md bg-green-50 p-4">
          <p class="text-sm text-green-800"><%= notice %></p>
        </div>
      </div>
    <% end %>

    <%# Main content %>
    <main class="mx-auto w-full max-w-3xl flex-1 px-4 py-8">
      <%= yield %>
    </main>

    <%# Footer %>
    <footer class="border-t border-gray-300 py-8">
      <div class="mx-auto max-w-3xl px-4 text-center text-sm text-gray-500">
        <p>&copy; <%= Date.current.year %> <%= site_name %>. All rights reserved.</p>
      </div>
    </footer>
  </body>
</html>
